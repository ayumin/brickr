# AGENTS.md

Brickr でエージェントが作業を始める前に読むファイルです。

このファイルは**案内役**です。ルールの実体は各ファイルにあります。
ここに内容を転記しないでください。二重管理になると必ず不一致します。

## 読む順序

1. **`LEARNED.md`** — 過去のセッションで実際に踏んだ落とし穴と、上手くいったやり方。
   Keep / Problem / Try / Notes の 4 区分。**作業を始める前に必ず読んでください。**
   ここを飛ばすと、過去に報告されたものと同じ失敗を繰り返します。
2. **`CLAUDE.md`** — プロダクト仕様の単一情報源。Room、Character、Post、Thread、SSE、
   ScheduledEvent などのドメイン定義と MVP の範囲はここが正。
3. **`CONTRIBUTE.md`** — 開発の進め方、設計上の約束、テスト方針、MR の書き方、
   ブラウザが必要な手動検証チェックリスト。
4. **`ARCHITECTURE.md`** — システム全体の構造。
5. **`.agents/skills/glab/SKILL.md`** — GitLab を操作する前に読む。`glab` の正しい使い方。

`docs/duo-sessions/` には振り返りの生ログがあります。`LEARNED.md` の項目の根拠を
辿りたいときだけ参照してください。通常の作業で読む必要はありません。

## 所有と更新の境界

| ファイル | 所有 | エージェントによる更新 |
|----------|------|----------------------|
| `docs/duo-sessions/YYYY-MM-DD.md` | 記録 | 追記のみ可。既存行を書き換えない |
| `LEARNED.md` | エージェント | 可。ファイル冒頭の更新ルールに従う |
| `CLAUDE.md`、`CONTRIBUTE.md`、`ARCHITECTURE.md` | 人間 | **不可**。必要なら MR で提案する |
| `.gitlab/duo/` | 人間 | **不可** |

振り返りで得た学びを `CLAUDE.md` へ直接書き込まないでください。
`LEARNED.md` に書き、恒久仕様への昇格は人間が判断します。

`.gitlab/duo/` を編集しないのは、ここにフロー定義とトリガー設定があるためです。
**自分の実行条件を自分で変更できる状態にしないでください。**

## 事故の多い項目

以下は `LEARNED.md` の要約です。項目 ID を添えてあるので、詳細と根拠は
そちらを見てください。食い違いがあった場合は `LEARNED.md` を正とします。

- **git 操作は常にリポジトリルートから**（P2）。pnpm workspace の monorepo なので、
  サブディレクトリからの `git diff` は変更があっても空を返します。
- **MR 説明と Issue コメントは一時ファイル経由で渡す**（K2）。インラインで組み立てると
  本文中のバッククォートがコマンド置換として展開されます。
- **テスト前に `pnpm --filter @brickr/backend db:generate` を実行**（P3）。未実行だと
  Prisma 未初期化で複数のテストファイルがロードに失敗します。自分の変更とは無関係です。
- **同じコマンドを繰り返さない**（P1）。2 度同じ失敗を返したら前提を疑い、
  アプローチを変えられないなら報告して止まってください。
- **バイナリアセットを伴うタスクは引き受けない**（P4）。画像を見られず、Issue の
  添付ファイルも API 経由では取得できません。着手前に報告して依頼者に戻してください。

## 検証

MR を出す前にリポジトリルートで実行します。

```shell
pnpm lint
pnpm test
pnpm typecheck
pnpm build
```

対象パッケージだけを検証するコマンドと、ブラウザが必要な確認項目は
`CONTRIBUTE.md` の「テスト」節にあります。

MR の説明は `.gitlab/merge_request_templates/default.md` の構成に合わせてください。
